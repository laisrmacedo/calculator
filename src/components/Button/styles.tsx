import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#000',
    height: 60,
    width: 60,
    borderRadius: 12,
    alignItems: 'center',
    justifyContent: 'center'
  },
  label: {
    color: '#fff',
    fontSize:20,
    fontWeight: 700
  },
  operator: {
    color: '#505050',
    fontSize:20,
    fontWeight: 700
  }
})