import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    height: '100%'
  },
  containerBtn:{
    width: '100%',
    flexDirection: 'row',
    // gap: 4,
    justifyContent: 'space-around'
    
  }
})